const initState = {
    captured: false,

};

var initialState = {
    count: 0
};

export function Weather(state = initState, action = {}) {
    switch (action.type) {
        default:
            return state;
    }
};